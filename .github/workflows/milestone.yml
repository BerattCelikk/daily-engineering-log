name: Milestone
on:
  issues:
    types: [opened]
jobs:
  milestone:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: github.rest.issues.update({...context.repo, issue_number: context.issue.number, milestone: 1})
